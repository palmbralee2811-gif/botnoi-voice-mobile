name: botnoivoice
description: "Botnoi Voice Mobile"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  # pick file from storage
  file_picker: ^4.0.3

  # text to voice generate and play audio
  # http: ^1.2.1
  http: ^0.13.3
  audioplayers: ^1.0.1

  # download and save file
  dio: ^4.0.0
  path_provider: ^2.0.0

  # open file
  open_file: ^3.2.1

  # ขออนุญาตการเข้าถึงไฟล์ใน Runtime
  permission_handler: ^10.0.0

  # เพื่อเปิดไฟล์โดยใช้ URL ตรงของไฟล์ที่ดาวน์โหลดมาใหม่ โดยไม่ต้องผ่านทาง Content URI
  url_launcher: ^6.0.0

  # firebase and flutterfire
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0

  # import function from other file
  provider: ^6.1.2
  
  # sign-in
  google_sign_in: ^6.2.1
  google_fonts: ^4.0.0

  # ไล่ แฉด สี
  gradient_borders: ^1.0.1

  # svg file import
  flutter_svg: ^0.22.0
  
  # เก็บข้อมูลจาก API ไว้ใน้ Cache ของ Mobile Device
  shared_preferences: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
        - assets/images/
        - assets/logo/

  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
