flutter clean && flutter pub get && flutter run 

flutter clean && flutter pub get
flutter build apk --release
flutter build appbundle --release

# Android (.apk) for Firebase App Tester Only
cd /Users/kawin101/Desktop/botnoi-voice-mobile/botnoivoice/
flutter clean && flutter pub get
flutter build apk --release

# Android (.aap) for Google Play Store
cd /Users/kawin101/Desktop/botnoi-voice-mobile/botnoivoice/
flutter clean && flutter pub get
flutter build appbundle --release

# How to Upload native debug symbols for App bundle
1. cd /botnoivoice/build/app/intermediates/merged_native_libs/release/mergeReleaseNativeLibs/out/lib
2. zip all files in /lib
3. open terminal and run zip -d Archive.zip "__MACOSX*"
- pwd /botnoivoice/build/app/intermediates/merged_native_libs/release/mergeReleaseNativeLibs/out/lib
4. upload zip file on Google Play Console
5. done

# Reset Firebase Config
https://firebase.flutter.dev/docs/cli/

# For Windows OS
keytool -list -v -alias androiddebugkey -keystore "%USERPROFILE%.android\debug.keystore" -storepass android -keypass android